name: Build & Release CLI

# Fires after the auto-tag workflow tags every push to main/master.
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build pdfcli
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    outputs:
      asset_names: ${{ steps.assets.outputs.names }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          mkdir -p dist
          bin="pdfcli-${{ matrix.goos }}-${{ matrix.goarch }}"
          if [[ "${{ matrix.goos }}" == "windows" ]]; then
            bin="${bin}.exe"
          fi
          go build -trimpath -ldflags="-s -w" \
            -o "dist/${bin}" ./cmd/pdfcli
          echo "dist/${bin}" >> dist/assets.txt

      - name: Record asset names
        id: assets
        shell: bash
        run: |
          names="$(cat dist/assets.txt)"
          names="${names//$'\n'/,}"
          echo "names=${names}" >> "$GITHUB_OUTPUT"

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: pdfcli-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*.exe
          if-no-files-found: ignore
          retention-days: 1

  release:
    name: Publish release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download binaries
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create / update GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF_NAME}"
          assets=($(find dist -type f -name "pdfcli-*" | sort))
          echo "Tag: ${tag}"
          echo "Assets:"
          printf '  %s\n' "${assets[@]}"

          if gh release view "${tag}" >/dev/null 2>&1; then
            echo "Release ${tag} exists; uploading assets."
            gh release upload "${tag}" "${assets[@]}" --clobber
          else
            echo "Creating release ${tag}."
            gh release create "${tag}" "${assets[@]}" \
              --title "pdfcli ${tag}" \
              --notes "Automated build of the pdfcli CLI for tag ${tag}."
          fi
